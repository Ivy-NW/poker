/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // You can add other Next.js configurations here if needed.
  // For example, environment variables, redirects, rewrites, etc.
};

module.exports = nextConfig;
